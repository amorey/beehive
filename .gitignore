.worktrees

# Claude Code state is per-machine by default: only .claude/settings.json is shared,
# so the directory is ignored wholesale and that one file is re-admitted. The
# .local.json overlay (one developer's permission grants) must not follow the repo.
.claude/*
!.claude/settings.json
